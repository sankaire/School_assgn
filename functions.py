from squares import square  # importing a module

# or you can import the whole module like, import squares

for i in range(1, 10):
    print(
        f"the square of {i} is {square(i)}"
    )  # when you import thr whole module you have to use squares.square
